version: '3.8'

services:
  sat-solver:
    build: .
    container_name: sat_solver_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
